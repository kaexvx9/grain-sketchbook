//! Kernel panic output.
//! Why: Minimal panic handler for kernel crashes.

const std = @import("std");
const RawIO = @import("uart.zig");

/// Output panic message to UART.
/// Why: Debugging kernel crashes requires immediate UART output.
pub fn write(msg: []const u8) void {
    RawIO.write(msg);
}
