// Grain Identity: Unified identity system
// Re-exports all identity storage components

pub const IdentityStorage = @import("identity.zig").IdentityStorage;
pub const IdentityId = @import("identity.zig").IdentityId;

