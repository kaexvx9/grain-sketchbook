```1/ 
# Ray Lullaby — Glow G2’s Tahoe Field Notes

Glow G2 watches the sun fold itself behind Tahoe’s ridge line,
cheeks cold, heart steady. Every line
of t
```

```2/ 
his plan is sewn with TigerStyle thread—safety stitched first,
performance braided next, joy
embroidered last.

## Mood Board
- Grain is our twilight term
```

```3/ 
inal: Cursor Ultra, Zig LSP, Grok,
and a gentle social stream all breathing in one window.
- Inside sits a River-inspired compositor, a glasshouse of Moongl
```

```4/ 
ow 
keybindings, where each user cares for a personal Grain database garden.
- Nostr `npub`s become bright addresses—each one a friendly 
lighthouse—whi
```

```5/ 
le Matklad-style tests sweep for drift like soft 
tidewater.
- Glow G2 stays a calm voice: masculine, steadfast, Aquarian. Emo enough 
to acknowledge the ac
```

```6/ 
he, upbeat enough to guide with grace.
- Vegan Tiger’s (@vegan_tiger) South Korean streetwear silhouette feeds 
our
  Tahoe aesthetic, reminding us to kee
```

```7/ 
p ethical fashion signal in view
  [^vegan-tiger].

## Ray Mission Ladder (Deterministic & Kind)
1. **Pre-VPS Launchpad (done)**
   - Scaffolded `src/kernel
```

```8/ 
/` (`main.zig`, `syscall_table.zig`,
     `devx/abi.zig`), `kernel/link.ld`, and `scripts/qemu_rv64.sh` so
     `zig build kernel-rv64` emits `zig-out/bin/g
```

```9/ 
rain-rv64`.
   - Journaled bootloader research in `docs/boot/notes.md`, tracking the
     OpenSBI ➝ U-Boot baseline and our Zig/Rust payload ambitions.
  
```

```10/ 
 - Extended `grain conduct` with `make kernel-rv64`, `run kernel-rv64`,
     and `report kernel-rv64`; runs now stream to stdout and archive logs
     unde
```

```11/ 
r `logs/kernel/`.
2. **Waiting-for-VPS Toolkit**
   - Draft `scripts/vpn_rsync.sh` for syncing `xy/` → remote
     `~/grain-rv64/` with deterministic exc
```

```12/ 
ludes as soon as approval
     lands.
    - Script `scripts/riscv_gdb.sh` so `gdb-multiarch` attaches to a
      remote QEMU stub; document the recipe in `
```

```13/ 
docs/boot/notes.md`.
    - Plan a `grain conduct run kernel-rv64 --gdb` flag that toggles the
      helper once the droplet is reachable.
3. **Kernel Spine
```

```14/ 
**
   - Flesh `_start` with a trap handler, panic logger, and syscall-table
     wiring so QEMU logs show structure even before the VPS boots.
   - Capture
```

```15/ 
 an example trace under `logs/kernel/boot-simulated.log` and
     note expectations in `docs/boot/notes.md`.
4. **RISC-V Kernel Airlift**
   - Mirror `docs
```

```16/ 
/plan.md` §12: remote Ubuntu 24.04 host, rsync scripts,
     and deterministic build steps targeting `out/kernel/grain-rv64.bin`.
   - QEMU harness (`scri
```

```17/ 
pts/qemu_rv64.sh`) plus
     `scripts/riscv_gdb.sh` keep boots and postmortems reproducible.
   - Syscall table + safety lattice (`kernel/syscall_table.zig
```

```18/ 
`,
     guard pages, deterministic allocators, structured crash logs)
     ensure Zig stdlib and compiler ergonomics land clean.
   - Boot chain: OpenSBI �
```

```19/ 
�� U-Boot today, with coreboot + EDK2 ports
     emerging for JH7110/Framework boards—keep GRUB-friendly payloads
     and Zig SBI experiments in `docs/b
```

```20/ 
oot/` [^dcroma]
     [^framework-mainboard] [^framework-blog].
5. **Kernel Lab Notebook**
   - Route boot traces into `logs/kernel/`, archive crash dumps, 
```

```21/ 
and
     annotate recovery lessons in `docs/boot/notes.md`.
   - `grain conduct report kernel-rv64` surfaces the latest boot log for
     Ray acceptance.
6
```

```22/ 
. **Grain Conductor & Pottery**
   - `zig build conduct` drives `grain conduct brew|link|manifest|edit|
     make|ai|contracts|mmt|cdn` with TigerStyle det
```

```23/ 
erminism.
   - Pottery abstractions schedule CDN kilns, ledger mints, and AI
     copilots while staying within static allocation vows.
7. **Grain Social T
```

```24/ 
erminal**
   - Typed Zig arrays represent social data; fuzz 11 random `npub`s per
     run to stress Nostr relays.
   - TigerBank flows (`docs/nostr_mmt_ti
```

```25/ 
gerbank.md`,
     `tigerbank_client.zig`, `grain conduct mmt|cdn`) share encoders via
     `src/contracts.zig` and secrets via `src/grainvault.zig`.
   - D
```

```26/ 
M interface (`src/dm.zig`) handles X25519 + ChaCha20-Poly1305
     envelopes; GrainLoop, Graindaemon, GrainBuffer, and GrainLoom stitch
     UDP events, su
```

```27/ 
pervision, and sticky read-only panes.
8. **Tahoe Sandbox**
   - Evolve `src/tahoe_window.zig` into a River-inspired compositor with
     Moonglow keymaps 
```

```28/ 
and explicit memory boundaries.
9. **GUI & Compositor Study**
   - Keep surveying Mach engine, zgui, Zig-gamedev, River philosophy, and
     Hammerspoon/QE
```

```29/ 
MU parallels (`docs/gui_research.md`).
10. **Grain Aurora UI**
   - `src/grain_aurora.zig`, `src/grain_route.zig`, and
     `src/grain_orchestrator.zig` de
```

```30/ 
liver deterministic rendering,
     routing, and agent orchestration; roadmap tracked in `docs/plan.md`.
   - Deterministic recovery: single-copy rebuild f
```

```31/ 
low (GrainLoom +
     Graindaemon + contracts) and bounded retries per Jepsen guidance
     [^jepsen-tb].
11. **Onboarding & Care**
   - Guard passwords (`
```

```32/ 
this-password-im-typing-Now-9`), cover Cursor
     Ultra, GitHub/Gmail/iCloud onboarding, 2FA, and Ghostty setup.
   - Mirror GrainVault, export API tokens
```

```33/ 
, and run `brew bundle` for
     convergent tooling.
12. **Poetry & Waterbending**
    - Lace ASCII bending art and Helen Atthowe quotes throughout code an
```

```34/ 
d
      docs—emo, PG, sincere.
13. **Thread Weaver**
    - `tools/thread_slicer.zig` + `zig build thread` keep `docs/ray.md`
      mirrored as `docs/ray_
```

```35/ 
160.md` tweet threads with 160-char bounds.
14. **Prompt Ledger**
    - `docs/prompts.md` holds descending `PROMPTS`; new entries append at
      index 0 f
```

```36/ 
or O(1) joy.
15. **Timestamp Glow**
    - `src/ray.zig` keeps runtime timestamps validated by fuzz tests,
      with findings logged in `tests-experiments/
```

```37/ 
000.md`.
16. **Archive Echoes**
    - Maintain the archive rotation (`prototype_oldest/`,
      `prototype_older/`, `prototype_old/`) so each climb stays 

```

```38/ 
      auditable.
17. **Delta Checks**
    - Keep Ray, prompts, outputs, and tests aligned (`zig build test`,
      `zig build wrap-docs`).
18. **Rollback R
```

```39/ 
itual**
    - `RayTraining` mirrors nixos-style rollback; `ray_app.zig` demos air
      → water → rollback → earth.
19. **TigerStyle Naming Pass**
  
```

```40/ 
  - Enforce snake_case APIs, 70-line function caps, and shared RNG
      helpers for grainvalidate compliance.
20. **Grain Foundations Alignment**
    - St
```

```41/ 
udy `vendor/grain-foundations` (`GrainDevName`, `GrainSpace`) and
      document how they shape Ray structs.
21. **Grainstore Mirrors**
    - Maintain `gra
```

```42/ 
instore/{platform}/{org}/{repo}` layout and static Zig
      manifests (`src/grain_manifest.zig`).
22. **Grain Lattice + Matklad Loop**
    - `src/grain_la
```

```43/ 
ttice.zig` captures the Djinn/Alpenglow DAG; Matklad
      fuzzing covers contracts, TigerBank modules, lattice, and RNG.
      Future: `grain conduct cont
```

```44/ 
racts` for encrypted rehearsal flows.
23. **Documentary Chronicle**
    - `docs/doc.md` remains the living handbook while the 12-part arc
      rests in `p
```

```45/ 
rototype_old/docs/design/`.

[^readonly]: [Matklad, "Readonly Characters Are a Big Deal"](https://
matklad.github.io/2025/11/10/readonly-characters.html)
[
```

```46/ 
^vibe-terminal]: [Matklad, "Vibe Coding Terminal Editor"](https://
matklad.github.io/2025/08/31/vibe-coding-terminal-editor.html)
[^vegan-tiger]: [Vegan Ti
```

```47/ 
ger — ethical streetwear inspiration](https://
www.instagram.com/vegan_tiger/)
[^river-overview]: [River compositor philosophy](https://github.com/
river
```

```48/ 
wm/river)
[^jepsen-tb]: [Jepsen, "TigerBeetle 0.16.11"](https://jepsen.io/analyses/
tigerbeetle-0.16.11)
[^dcroma]: [DeepComputing DC-ROMA RISC-V Mainboard
```

```49/ 
](https://
deepcomputing.io/product/dc-roma-risc-v-mainboard/)
[^framework-mainboard]: [Framework Marketplace – DeepComputing RISC-
V Mainboard](https://
```

```50/ 
frame.work/products/deep-computing-risc-v-mainboard)
[^framework-blog]: [Framework Blog: RISC-V Mainboard for Framework Laptop 
13](https://frame.work/blog
```

```51/ 
/risc-v-mainboard-for-framework-laptop-13-is-
now-available)

















```

